/**
 * Pilot Mode Feature Configuration
 * 
 * Central switch for all Pilot Mode banners, forms, and feedback flows.
 * Set to `false` whenever Pilot Mode should be completely removed/disabled across the platform.
 */
export const PILOT_MODE_ENABLED = true;
